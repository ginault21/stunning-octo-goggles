{
  "image": "mcr.microsoft.com/devcontainers/javascript-node:20",
  "postCreateCommand": "npm install && npm run setup && npm install -g opencode-ai && chmod +x .devcontainer/setup-opencode.sh && .devcontainer/setup-opencode.sh",
  "forwardPorts": [3000],
  "remoteEnv": {
    "NODE_ENV": "development"
  }
}
